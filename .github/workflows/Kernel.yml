name: "S25U-SukiSU-Ultra-6.6-Global-Pack"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 检出代码
        uses: actions/checkout@v4

      - name: ⚙️ 准备环境
        run: |
          sudo apt update && sudo apt install -y zip cpio python3 inotify-tools patch
          chmod +x kernel_platform/build/kernel/build.config.sh || true
          touch hijack.log && chmod 666 hijack.log

      - name: 🧬 注入 SukiSU-Ultra & 官方 SUSFS v2.0 (Android 15 分支适配)
        run: |
          cd kernel_platform/common
          
          # 1. 注入 SukiSU-Ultra 核心 (创建 drivers/kernelsu 目录)
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s builtin
          
          # 2. 克隆指定的 Android 15 分支
          echo "📥 克隆 SUSFS gki-android15-6.6 分支..."
          git clone --depth=1 -b gki-android15-6.6 https://gitlab.com/simonpunk/susfs4ksu.git /tmp/susfs
          
          # 3. 【官方流程】分发补丁与源码文件
          echo "📦 正在按照官方规范分发文件..."
          # 拷贝 KSU 专用补丁
          cp /tmp/susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch ./drivers/kernelsu/
          
          # 拷贝内核核心补丁 (针对 6.6)
          cp /tmp/susfs/kernel_patches/50_add_susfs_in_kernel-6.6.patch ./
          
          # 拷贝 fs 和 include 目录下的系统级修改
          cp -r /tmp/susfs/kernel_patches/fs/* ./fs/
          cp -r /tmp/susfs/kernel_patches/include/linux/* ./include/linux/
          
          # 4. 【官方流程】应用双重补丁
          echo "🩹 正在应用 SUSFS 补丁..."
          
          # 补丁 1: KernelSU 适配
          cd drivers/kernelsu
          patch -p1 < 10_enable_susfs_for_ksu.patch || echo "⚠️ KSU 补丁冲突"
          
          # 补丁 2: 内核主体适配
          cd ../../
          patch -p1 < 50_add_susfs_in_kernel-6.6.patch || echo "⚠️ 内核补丁冲突"

          # 5. Makefile 保险
          if ! grep -q "susfs.o" drivers/kernelsu/Makefile; then
              echo "obj-y += susfs.o" >> drivers/kernelsu/Makefile
          fi

          # 6. 【Android 15 关键】移除 ABI 保护文件
          # 官方强调：不删这两个文件会导致 WiFi/蓝牙 等模块在 Android 14/15 上失效
          echo "🚫 移除 ABI 保护以确保硬件驱动正常..."
          rm -f android/abi_gki_protected_exports_aarch64
          rm -f android/abi_gki_protected_exports_x86_64

          echo "✅ 源码注入与 Android 15 适配完成"

      - name: 🛰️ 启动后台劫持引擎
        run: |
          sudo stdbuf -oL -eL ./kernel_platform/build/kernel/build.config.sh > hijack.log 2>&1 &
          echo "HIJACK_PID=$!" >> $GITHUB_ENV

      - name: 🏗️ 执行内核编译
        run: |
          (
            LAST_CONTENT=""
            while true; do
              CURRENT_CONTENT=$(cat hijack.log 2>/dev/null)
              if [ "$CURRENT_CONTENT" != "$LAST_CONTENT" ]; then
                echo -e "\n--- [劫持战报] ---"
                diff <(echo "$LAST_CONTENT") <(echo "$CURRENT_CONTENT") | grep '^>' | sed 's/^> //' || echo "$CURRENT_CONTENT"
                LAST_CONTENT="$CURRENT_CONTENT"
              fi
              sleep 5
            done
          ) &
          MONITOR_PID=$!
          export LOCALVERSION=""
          yes "" | ./build_kernel_GKI.sh pa3q_chn_openx user sun
          kill $MONITOR_PID || true

      - name: 🛠️ 提取 DTBO (文件名去重模式)
        if: success()
        run: |
          mkdir -p dtbo_work
          # 锁定根目录的 out 和 drivers
          find out -name "*.dtbo" -type f 2>/dev/null | while read -r file; do
              fname=$(basename "$file")
              if [ ! -f "dtbo_work/$fname" ]; then cp "$file" "dtbo_work/$fname"; fi
          done
          find drivers -name "*.dtbo" -type f 2>/dev/null | while read -r file; do
              fname=$(basename "$file")
              if [ ! -f "dtbo_work/$fname" ]; then cp "$file" "dtbo_work/$fname"; fi
          done

          if [ -n "$(ls -A dtbo_work 2>/dev/null)" ]; then
              ./kernel_platform/prebuilts/kernel-build-tools/linux-x86/bin/mkdtboimg create dtbo.img dtbo_work/*.dtbo || true
          fi
          
          GLOBAL_IMAGE=$(find out -name "Image" | grep "sun_perf" | head -n 1)
          [ -z "$GLOBAL_IMAGE" ] && GLOBAL_IMAGE=$(find . -name "Image" -type f | grep -v "AnyKernel" | head -n 1)
          
          git clone --depth=1 https://github.com/fei-ke/AnyKernel3.git -b s25 ak3_pack
          [ -n "$GLOBAL_IMAGE" ] && cp "$GLOBAL_IMAGE" ak3_pack/
          [ -f "dtbo.img" ] && cp dtbo.img ak3_pack/
          cd ak3_pack && zip -r9 ../S25U-SukiSU-Kernel-Flashable.zip ./*
          cd ..

      - name: 📦 连根拔起：全量归档原始目录
        if: always()
        run: |
          mkdir -p final_artifacts
          [ -d "out" ] && zip -r9 final_artifacts/out_full_archive.zip out/ || echo "out missing"
          [ -d "drivers" ] && zip -r9 final_artifacts/drivers_full_archive.zip drivers/ || echo "drivers missing"
          cp S25U-SukiSU-Kernel-Flashable.zip final_artifacts/ 2>/dev/null || true
          cp hijack.log final_artifacts/ 2>/dev/null || true

      - name: 📤 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: S25U-SukiSU-Ultra-Final-Pack
          path: final_artifacts/

