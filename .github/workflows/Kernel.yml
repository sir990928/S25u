name: "S25U-SukiSU-Ultra-Clean-NoSUSFS"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 检出代码
        uses: actions/checkout@v4

      - name: ⚙️ 准备环境
        run: |
          sudo apt update && sudo apt install -y zip cpio python3 patch
          chmod +x ./build_kernel_GKI.sh
          chmod +x ./kernel_platform/build/kernel/build.config.sh || true

      - name: 🧬 注入 SukiSU-Ultra (不含 SUSFS)
        run: |
          cd kernel_platform/common
          
          # 1. 仅注入 SukiSU 核心
          echo "🛠️ 注入 SukiSU 核心..."
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s builtin
          
          # 2. 移除 ABI 保护 (WiFi 修复关键)
          rm -f android/abi_gki_protected_exports_*
          echo "✅ SukiSU 注入完成，SUSFS 已彻底排除"

      - name: 🏗️ 后台劫持与前台编译
        run: |
          # 启动后台引擎 (确保你的 build.config.sh 里没有 SUSFS 的 CONFIG)
          nohup sudo ./kernel_platform/build/kernel/build.config.sh > /dev/null 2>&1 &
          HIJACK_PID=$!
          
          # 执行唯一编译进程
          export LOCALVERSION=""
          yes "" | ./build_kernel_GKI.sh pa3q_chn_openx user sun
          
          kill $HIJACK_PID || true

      - name: 🛠️ 产物提取与合成 (仅 Image + DTBO)
        if: always()
        run: |
          mkdir -p final_artifacts
          
          # 1. DTBO 合并逻辑
          mkdir -p dtbo_work
          declare -A seen_hashes
          find out drivers -name "*.dtbo" -type f 2>/dev/null | while read -r dtbo_file; do
              hash=$(md5sum "$dtbo_file" | awk '{print $1}')
              if [ -z "${seen_hashes[$hash]}" ]; then
                  seen_hashes[$hash]=1
                  cp "$dtbo_file" "dtbo_work/${hash}.dtbo"
              fi
          done
          
          if [ -d "dtbo_work" ] && [ "$(ls -A dtbo_work)" ]; then
              ./kernel_platform/prebuilts/kernel-build-tools/linux-x86/bin/mkdtboimg create dtbo.img dtbo_work/*.dtbo || true
          fi

          # 2. 准备卡刷包
          GLOBAL_IMAGE=$(find out -name "Image" | grep "sun_perf" | head -n 1)
          if [ -n "$GLOBAL_IMAGE" ]; then
              git clone --depth=1 https://github.com/fei-ke/AnyKernel3.git -b s25 ak3_pack
              cp "$GLOBAL_IMAGE" ak3_pack/
              [ -f "dtbo.img" ] && cp dtbo.img ak3_pack/
              cd ak3_pack && zip -r9 ../final_artifacts/S25U-Flashable-NoSUSFS.zip ./* && cd ..
          fi

      - name: 📤 分开上传：卡刷包
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S25U-Flashable-NoSUSFS
          path: final_artifacts/S25U-Flashable-NoSUSFS.zip

      - name: 📤 连根拔起：上传全量 out
        if: always()
        run: zip -r1 out_full.zip out/
      
      - name: 🚀 上传 out
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S25U-Root-Out
          path: out_full.zip

      - name: 📤 连根拔起：上传全量 drivers
        if: always()
        run: zip -r1 drivers_full.zip drivers/

      - name: 🚀 上传 drivers
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: S25U-Root-Drivers
          path: drivers_full.zip

